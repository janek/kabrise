import React from "react"
import styled from "styled-components"

// TODO: colors
// TODO: height as variable for height and radius (XXX: consider switching to emotion before that )

const Button = styled.button`
  border: 1px solid black;
  height: 30px;
  border-radius: 15px;
  padding: 2px 20px;
`

export default Button
